var tempurature= 80 
    if (tempurature<50){ //Condition for executing the function
        document.write('You need a coat'); //Output if temp is less that 50
    }
    if (tempurature>=50){  //Condition for executing the function
        document.write('You do not need a coat'); //Output is temp is more than or equal to 50
    }