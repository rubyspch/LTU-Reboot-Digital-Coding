function showFullName(fName, sName){ //This is the declaration
    var fullName = fName + sName //The variable defined using the placeholder arguments
    document.write(fullName + '<br>'); //Output the full name
}
showFullName('Ruby ','Chohan'); // This is the arguments to be applied to the function
showFullName('Matthew ', 'Williams');