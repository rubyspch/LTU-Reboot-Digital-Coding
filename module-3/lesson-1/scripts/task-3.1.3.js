var NumberOfKittens= "5"; // This defines the variable

alert(NumberOfKittens); // Creates a pop up when the page loads that displays the number we defined

console.log(NumberOfKittens); // Creates a message in the console when a page is inspected showing the number that defined the variable

document.write(NumberOfKittens); // Adds content to the page showing the defined variable value